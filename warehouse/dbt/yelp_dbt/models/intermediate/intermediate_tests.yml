version: 2

models:
  - name: int_reviews_deduped
    columns:
      - name: review_id
        tests: [not_null, unique]
      - name: business_id
        tests: [not_null]
      - name: user_id
        tests: [not_null]